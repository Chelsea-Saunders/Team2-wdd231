# System files (Mac)
.DS_Store

# VS Code settings (local-only)
.vscode/

# Node dependencies (if using Node tools like Vite or npm)
node_modules/

# Environment/API key files (if you use .env or config files)
.env

# Build output folders (like with Vite, Parcel, etc.)
dist/
build/

# Logs (if using build tools or debug logging)
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Optional: ignore anything secret or local only
secrets.json
